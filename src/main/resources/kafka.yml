sandwich:
  kafka:
    bootstrap-servers: ${SANDWICH_KAFKA_SERVER:http://localhost:9092}
    security.protocol: ${SANDWICH_KAFKA_SECURITY_PROTOCOL:PLAINTEXT}
    producer:
      topic:
        order: ${SANDWICH_ORDER_TOPIC:sandwich-order}

billing-service:
  kafka:
    excluded-consumers: ${EXCLUDED_CONSUMERS:}